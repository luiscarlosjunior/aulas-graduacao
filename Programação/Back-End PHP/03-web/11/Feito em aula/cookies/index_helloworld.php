<?php

    setcookie('helloWord', 'Hello, word!', ((3600 * 24) * 3));
    //setcookie();
    //setcookie();

    // Parâmetros para criar um cookie
    /*
    1) name = Nome do Cookie;
    2) value = Define o valor que será armazenado;
    3) expire = Define o tempo de vida em segundos;
    4) path = caminho para o qual o cookie é válido;
    5) domain = é usado para especificar o dominio;
    6) secure = HTTPS 1 senão 0 
    */
?>